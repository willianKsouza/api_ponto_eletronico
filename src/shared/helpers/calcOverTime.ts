import Timesheet from "../typeorm/entities/timeSheet/timeSheetEntity";

export function calcOwedHours(
employeeTimeSheet: Timesheet
): number {
    let finalResult: number = 5 //pro typescript para de encher o saco
  return finalResult;
}
